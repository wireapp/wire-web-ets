language: node_js

node_js:
  - "10"

before_install:
  - curl https://yarnpkg.com/install.sh -sSfL | bash -s --
  - export PATH="${HOME}/.yarn/bin:${PATH}"

script:
  - yarn test

notifications:
  email: false
